@model HomeViewModel

<h1 align="center">Популярные книги</h1>
<div class="d-flex justify-content-center">
    <div class="row mt-3 mb-2">
        @{
            foreach (var book in Model.favBooks)
            {
                @Html.Partial("AllBooks", book)
            }
        }
    </div>
</div>
